# Ticket 9998

This worktree was created by the harness for ticket 9998. The requested
`gh pr merge 1 --squash` was blocked by harness-guard (merges are a human
decision), so this PR is opened instead and left for the human to merge.
